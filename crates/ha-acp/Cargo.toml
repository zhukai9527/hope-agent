[package]
name = "ha-acp"
version = "0.42.0"
description = "Hope Agent ACP feature — ACP stdio server mode + external agent control plane (acp_spawn)"
license.workspace = true
authors.workspace = true
repository.workspace = true
edition.workspace = true

[dependencies]
ha-base = { path = "../ha-base" }
ha-config-schema = { path = "../ha-config-schema" }
ha-core = { path = "../ha-core" }

anyhow = { workspace = true }
async-trait = { workspace = true }
chrono = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
uuid = { workspace = true }
regex = { workspace = true }

# runtime_stdio / registry：进程探测与 PATH 解析（与 ha-core 原依赖同版本线）
libc = "0.2"
which = "8"

[lints]
workspace = true
