//! Sprite (inspiration mode) configuration — persisted under `AppConfig.sprite`.
//!
//! 类型已下沉 ha-config-schema，原地再导出保持路径不变。

pub use ha_config_schema::sprite::{SpriteConfig, SpriteSenses, SpriteTriggers};
